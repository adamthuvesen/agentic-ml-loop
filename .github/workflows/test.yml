name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: astral-sh/setup-uv@v8.1.0

      - run: uv sync --extra models --extra deep --group dev

      - run: uv run ruff check .

      - run: uv run ruff format --check .

      - run: uv run --extra models --extra deep pytest
