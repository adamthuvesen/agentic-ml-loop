# Model Search Experiment

## Title

Bootstrap demo — trial conversion (synthetic)

## Goal

Smoke-test the agentic-ml-loop stack (runner, validation, loop prompts, learnings hooks) on a
small bundled dataset with a trivial baseline and a minimal linear model.

## Baseline

**Majority-class** predictor: every row gets the training-set positive rate.

## Dataset

Bundled CSV at `lib/demo_bootstrap/data/demo_events.csv`, generated by
`lib/demo_bootstrap/generate_data.py`.

## Data Profile

- **Row count**: 800
- **Feature count**: 4 (3 numeric + 1 categorical)
- **Feature types**: numeric engagement counts, categorical `segment`
- **Missing values**: none in generated data
- **Class balance**: roughly 35–45% positive (synthetic logit)

## Target

`converted`

## Problem Type

binary classification

## Split Strategy

Time-ordered 60% train / 20% validation / 20% test on `event_date` (no shuffling).

## Objective Metric

Validation ROC-AUC (`val_auc`).

## Candidate Families

- constant / majority baseline
- logistic regression (numeric + one-hot)

## Constraints

- deterministic bundled data
- no external services
- no leakage: `user_id` excluded from features

## Success Definition

- `majority-baseline` and `logreg-tiny` both run and appear in `results.json`
- validation AUC for logistic regression above majority baseline

## Autonomous loop (validation demo)

This experiment is meant to exercise the full supervisor: prompts, cross-experiment
learnings, research signals, journal iteration, and bounded cycles.

Minimum loop cycles before EXPERIMENT_COMPLETE: 6

- Use `<promise>CYCLE_DONE</promise>` until at least six `## Cycle NNNN:` entries exist
  in `research_journal.md` (each cycle: one hypothesis, bounded work, honest write-up).
- Only then may you emit `<promise>EXPERIMENT_COMPLETE</promise>`, and only if the
  completion rigor checklist in the cycle prompt is satisfied.
- Stopping after one cycle defeats the purpose of this demo.
